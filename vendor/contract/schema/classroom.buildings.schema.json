{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "elecon.classroom.buildings",
  "title": "教学楼列表",
  "type": "object",
  "description": "classroom.available 的 discovery 伴生结果。见 ADR-019。",
  "properties": {
    "campus": {
      "type": "string",
      "description": "楼栋列表所属校区；缺失表示未按校区筛选或来源未提供。"
    },
    "term": {
      "type": "string",
      "description": "楼栋列表所属学期标识；缺失表示未按学期筛选或来源未提供。"
    },
    "items": {
      "type": "array",
      "description": "可用于空教室查询的教学楼列表；无可用楼栋时可为空数组。",
      "items": {
        "type": "object",
        "required": ["building"],
        "properties": {
          "campus": {
            "type": "string",
            "description": "教学楼所属校区；缺失表示来源未提供。"
          },
          "building": {
            "type": "string",
            "minLength": 1,
            "description": "教学楼展示名；禁止空串；未知填 \"-\""
          },
          "buildingId": {
            "type": "string",
            "description": "教学楼在校内系统中的标识或代码；缺失表示来源未提供。"
          },
          "roomCount": {
            "type": "integer",
            "minimum": 0,
            "description": "教学楼内教室数量，单位为间；缺失表示来源未提供。"
          }
        }
      }
    }
  }
}
