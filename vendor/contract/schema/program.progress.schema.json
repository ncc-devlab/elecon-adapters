{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "elecon.program.progress",
  "title": "培养方案进度",
  "type": "object",
  "properties": {
    "requiredCredits": { "type": "number", "description": "培养方案要求的总学分；可选，缺失表示学校不提供。" },
    "completedCredits": { "type": "number", "description": "已完成的总学分；可选，缺失表示学校不提供。" },
    "remainingCredits": { "type": "number", "description": "尚需完成的总学分；可选，缺失表示学校不提供。" },
    "status": { "type": "string", "enum": ["inProgress", "completed", "unknown"], "description": "培养方案完成状态：inProgress=进行中，completed=已完成，unknown=未知；可选，缺失表示学校不提供。" },
    "groups": {
      "type": "array",
      "description": "培养方案分组进度列表；可选，缺失表示学校不提供。",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "培养方案分组名称；可选，缺失表示学校不提供。" },
          "requiredCredits": { "type": "number", "description": "该分组要求的学分；可选，缺失表示学校不提供。" },
          "completedCredits": { "type": "number", "description": "该分组已完成的学分；可选，缺失表示学校不提供。" }
        }
      }
    }
  }
}
