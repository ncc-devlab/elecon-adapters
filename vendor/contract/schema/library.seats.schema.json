{
  "$schema": "https://json-schema.org/draft/2020-12/schema", "$id": "elecon.library.seats", "title": "图书馆座位", "type": "object",
  "properties": { "items": { "type": "array", "description": "图书馆座位列表；省略表示源站不提供座位数据。", "items": { "type": "object", "properties": { "library": { "type": "string", "description": "座位所在图书馆；省略表示源站不提供。" }, "floor": { "type": "string", "description": "座位所在楼层；省略表示源站不提供。" }, "seatId": { "type": "string", "description": "座位的校内作用域标识；省略表示源站不提供。" }, "status": { "type": "string", "enum": ["available", "occupied", "reserved", "unknown"], "description": "座位状态：可用、已占用、已预约或未知；省略表示源站不提供。" }, "availableFrom": { "type": "string", "format": "date-time", "description": "座位可用时段的开始时刻，RFC3339/UTC；省略表示源站不提供。" }, "availableUntil": { "type": "string", "format": "date-time", "description": "座位可用时段的结束时刻，RFC3339/UTC；省略表示源站不提供。" } } } } }
}
