{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "elecon.dorm.service",
  "title": "宿舍服务",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "description": "宿舍服务记录列表；可选，缺失表示学校不提供。",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "校内作用域的宿舍服务记录标识；可选，缺失表示学校不提供。" },
          "type": { "type": "string", "description": "宿舍服务类型；可选，缺失表示学校不提供。" },
          "description": { "type": "string", "description": "宿舍服务事项描述；可选，缺失表示学校不提供。" },
          "status": { "type": "string", "enum": ["submitted", "processing", "completed", "cancelled", "unknown"], "description": "服务状态：submitted=已提交，processing=处理中，completed=已完成，cancelled=已取消，unknown=未知；可选，缺失表示学校不提供。" },
          "updatedAt": { "type": "string", "format": "date-time", "description": "服务记录最近更新时间，RFC3339/UTC；可选，缺失表示学校不提供。" }
        }
      }
    }
  }
}
