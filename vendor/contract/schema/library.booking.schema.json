{
  "$schema": "https://json-schema.org/draft/2020-12/schema", "$id": "elecon.library.booking", "title": "图书馆预约", "type": "object",
  "properties": { "items": { "type": "array", "description": "预约记录列表；省略表示源站不提供预约记录。", "items": { "type": "object", "properties": { "id": { "type": "string", "description": "预约记录的校内作用域标识；省略表示源站不提供。" }, "library": { "type": "string", "description": "预约所在图书馆；省略表示源站不提供。" }, "room": { "type": "string", "description": "预约所在房间或区域；省略表示源站不提供。" }, "seatId": { "type": "string", "description": "预约座位的校内作用域标识；省略表示源站不提供。" }, "startAt": { "type": "string", "format": "date-time", "description": "预约开始时刻，RFC3339/UTC；省略表示源站不提供。" }, "endAt": { "type": "string", "format": "date-time", "description": "预约结束时刻，RFC3339/UTC；省略表示源站不提供。" }, "status": { "type": "string", "enum": ["reserved", "cancelled", "checkedIn", "expired", "unknown"], "description": "预约状态：已预约、已取消、已签到、已过期或未知；省略表示源站不提供。" } } } } }
}
